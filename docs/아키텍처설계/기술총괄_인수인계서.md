# 터미널 프로토타입 - 기술 총괄 인수인계서

**작성일**: 2025-11-05
**아키텍처 설계자**: Architecture Team
**수신**: 기술 총괄자 (Technical Lead)
**프로젝트**: 고구리 터미널 프로토타입 Phase CLI-1

---

## 📋 인수인계 개요

### PM 최종 승인 사항
✅ **원래 아키텍처 100% 승인**
- 레이어 구조: 4-layer (Presentation → Business → Algorithm → Data)
- 디자인 패턴: Memento + Observer + Strategy(선택)
- 파일 구조: `prototype/` 기반 Clean Architecture
- 클래스 수: 9개
- 일정: 3-4주 (품질 최우선)

### 변경 사항
**없음** - 원래 설계 그대로 구현

---

## 🏗️ 아키텍처 개요

### 1. 레이어 구조

```
┌─────────────────────────────────┐
│   Presentation Layer            │
│   - TerminalUI                  │  ← 사용자 입출력
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│   Business Layer                │
│   - PathSwitchService           │  ← 비즈니스 로직
│   - CheckpointService           │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│   Algorithm Layer               │
│   - LCACalculator               │  ← 핵심 알고리즘
│   - PathReconstructor           │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│   Data Layer                    │
│   - Node                        │  ← 데이터 구조
│   - ConversationTree            │
│   - Store (+ Observer)          │
└─────────────────────────────────┘
```

**핵심 원칙**: 각 레이어는 하위 레이어만 의존

---

## 📦 컴포넌트 명세

### Data Layer

#### 1. Node (데이터 모델)
**책임**: 단일 대화 노드 데이터 보관

**인터페이스**:
```python
class Node:
    id: str
    parent_id: Optional[str]
    role: str  # 'user' | 'assistant' | 'system'
    content: str
    depth: int
    children_ids: List[str]
```

**제약사항**:
- `role`은 3가지 값만 허용
- `depth`는 0부터 시작 (루트 = 0)
- `children_ids`는 자동 관리 (캐시)

---

#### 2. ConversationTree (트리 관리)
**책임**: 노드 CRUD 및 경로 조회

**인터페이스**:
```python
class ConversationTree:
    def add_node(node: Node) -> None
    def get_node(node_id: str) -> Optional[Node]
    def get_children(node_id: str) -> List[Node]
    def get_path_to_root(node_id: str) -> List[str]
```

**제약사항**:
- 단일 루트만 허용
- 부모 노드가 존재해야 자식 추가 가능
- O(1) 노드 조회 (dict 기반)

---

#### 3. Store (상태 관리 + Observer)
**책임**: 전역 상태 관리 및 변경 알림

**인터페이스**:
```python
class Store:
    active_path: List[str]
    selected_node_id: Optional[str]
    checkpoints: List[Checkpoint]

    def subscribe(listener: Callable) -> None
    def set_active_path(path: List[str]) -> None  # Observer 알림 포함
    def set_selected_node(node_id: str) -> None   # Observer 알림 포함
```

**디자인 패턴**: Observer (경량 버전)
- `set_*` 호출 시 자동으로 구독자들에게 알림
- 리스너 에러는 전파하지 않음 (다른 리스너 보호)

**PM 승인 이유**:
- 반응형 UI의 핵심 (상태 변경 시 자동 출력)
- 웹 전환 시 React/Zustand와 동일한 패턴

---

### Algorithm Layer

#### 4. LCACalculator (LCA 계산)
**책임**: 두 노드의 최소 공통 조상 계산

**알고리즘**: 선형 방식 (O(h))
1. 두 노드의 깊이를 맞춤
2. 동시에 부모로 상승하여 공통 조상 찾기

**인터페이스**:
```python
class LCACalculator:
    @staticmethod
    def calculate(tree: ConversationTree, node_a_id: str, node_b_id: str) -> str
```

**테스트 시나리오** (필수 5가지):
1. 형제 노드 (D-F → LCA=B)
2. 사촌 노드 (D-G → LCA=A)
3. 부모-자식 (B-D → LCA=B)
4. 동일 노드 (D-D → LCA=D)
5. 루트 포함 (A-D → LCA=A)

**정확성 요구사항**: 100% (수학적 정확성 필수)

---

#### 5. PathReconstructor (경로 재구성)
**책임**: LCA 기반 새 경로 생성

**알고리즘**: Prefix + Suffix
1. Prefix: 루트 → LCA 경로
2. Suffix: LCA → 목표 노드 경로
3. 결합 (LCA 중복 제거)

**인터페이스**:
```python
class PathReconstructor:
    @staticmethod
    def reconstruct(tree: ConversationTree, lca_id: str, target_id: str) -> List[str]
```

**예시**:
- 현재: A→B→D, 목표: F
- LCA: B
- Prefix: [A, B]
- Suffix: [F]
- 결과: [A, B, F]

---

### Business Layer

#### 6. PathSwitchService (경로 전환)
**책임**: 경로 전환 비즈니스 로직 조율

**워크플로우**:
1. LCACalculator 호출 → LCA 계산
2. PathReconstructor 호출 → 새 경로 생성
3. Store 업데이트 → Observer 자동 알림

**인터페이스**:
```python
class PathSwitchService:
    def __init__(tree: ConversationTree, store: Store)
    def switch_path(current_leaf_id: str, target_node_id: str) -> List[str]
```

**특이사항**:
- 동일 노드 클릭 시 no-op
- Store 업데이트 시 Observer가 자동으로 UI 갱신

---

#### 7. Checkpoint (Memento)
**책임**: 불변 상태 스냅샷

**디자인 패턴**: Memento
- 상태를 `tuple`로 저장 (불변)
- 외부에서 수정 불가

**인터페이스**:
```python
@dataclass(frozen=True)
class Checkpoint:
    active_path: tuple  # 불변
    timestamp: datetime
    reason: str
```

---

#### 8. CheckpointService (체크포인트 관리)
**책임**: 체크포인트 생성/복원

**워크플로우**:
- 생성: 현재 `Store.active_path`를 Checkpoint로 저장
- 복원: Checkpoint → Store (Observer 알림 자동)

**인터페이스**:
```python
class CheckpointService:
    def __init__(store: Store)
    def create_checkpoint(reason: str) -> Checkpoint
    def restore_checkpoint(checkpoint: Checkpoint) -> None
    def list_checkpoints() -> List[Checkpoint]
```

---

### Presentation Layer

#### 9. TerminalUI (터미널 인터페이스)
**책임**: 사용자 입출력 및 명령어 라우팅

**Observer 연결**:
```python
def __init__(self, ...):
    self.store.subscribe(self.on_state_change)

def on_state_change(self, key, value):
    if key == 'active_path':
        print(f"✓ 경로 변경: {' → '.join(value)}")
```

**명령어**:
- `[t]` 트리 출력 (●/○ 하이라이트)
- `[p]` 활성 경로 출력
- `[m]` 대화 메시지 출력
- `[s]` 경로 전환
- `[c]` 체크포인트 생성
- `[r]` 체크포인트 복원
- `[q]` 종료

**인터페이스**:
```python
class TerminalUI:
    def __init__(tree, store, path_service, checkpoint_service)
    def main_loop() -> None
    def on_state_change(key, value) -> None  # Observer 콜백
```

---

## 📁 파일 구조

```
prototype/
├── main.py                      # 진입점
│
├── models/                      # 도메인 모델
│   ├── __init__.py
│   ├── node.py                  # Node
│   └── checkpoint.py            # Checkpoint
│
├── data/                        # 데이터 계층
│   ├── __init__.py
│   ├── tree.py                  # ConversationTree
│   └── store.py                 # Store (+ Observer)
│
├── algorithms/                  # 알고리즘 계층
│   ├── __init__.py
│   ├── lca.py                   # LCACalculator
│   └── path.py                  # PathReconstructor
│
├── services/                    # 비즈니스 로직
│   ├── __init__.py
│   ├── path_switch.py           # PathSwitchService
│   └── checkpoint.py            # CheckpointService
│
├── ui/                          # 프레젠테이션
│   ├── __init__.py
│   └── terminal.py              # TerminalUI
│
├── fixtures/                    # 샘플 데이터
│   ├── __init__.py
│   └── sample_tree.py           # A~G 샘플 트리
│
└── tests/                       # 테스트
    ├── __init__.py
    ├── test_tree.py
    ├── test_store.py
    ├── test_lca.py
    ├── test_path.py
    ├── test_path_switch.py
    ├── test_checkpoint.py
    └── test_integration.py
```

**총 파일**: 23개
**예상 코드량**: ~1200줄

---

## 🎨 디자인 패턴 정리

### 1. Memento (체크포인트)
**적용**: Checkpoint 클래스
**목적**: 상태를 불변 객체로 저장하여 안전한 복원
**구현 요구사항**:
- `@dataclass(frozen=True)` 사용
- `active_path`를 `tuple`로 저장 (불변)

### 2. Observer (상태 알림)
**적용**: Store 클래스
**목적**: 상태 변경 시 UI 자동 갱신
**구현 요구사항**:
- `subscribe(listener)` 메서드
- `set_*` 호출 시 자동으로 `_notify()` 호출
- 리스너 에러 격리 (try-except)

### 3. Strategy (선택적)
**적용**: LCACalculator (확장 가능성만 고려)
**목적**: 나중에 Binary Lifting 추가 가능
**현재 구현**: LinearLCA만 구현 (Strategy 인터페이스는 보류)

---

## ✅ 성공 기준

### 기능적 요구사항
- [ ] LCA 계산 정확성 100% (5가지 시나리오 통과)
- [ ] 경로 전환 동작 (형제/사촌/부모-자식)
- [ ] 체크포인트 저장/복원
- [ ] Observer 알림 자동 발생
- [ ] 터미널 UI 7개 명령어 동작

### 비기능적 요구사항
- [ ] 테스트 커버리지 85% 이상
  - Algorithm: 100%
  - Data: 95%
  - Business: 90%
  - UI: 70%
- [ ] 모든 함수에 docstring (Google Style)
- [ ] 타입 힌트 100% 적용
- [ ] PEP 8 준수

### 품질 요구사항
- [ ] 크리티컬 버그 제로
- [ ] 샘플 트리(A~G)에서 모든 경로 전환 성공
- [ ] 체크포인트 복원 시 상태 100% 복구
- [ ] Observer 알림 누락 없음

---

## 🧪 테스트 전략

### 필수 테스트 케이스

#### LCA 테스트
```python
tree = create_sample_tree()  # A-B-D-F, A-C-G

assert LCACalculator.calculate(tree, 'D', 'F') == 'B'  # 형제
assert LCACalculator.calculate(tree, 'D', 'G') == 'A'  # 사촌
assert LCACalculator.calculate(tree, 'B', 'D') == 'B'  # 부모-자식
assert LCACalculator.calculate(tree, 'D', 'D') == 'D'  # 동일
assert LCACalculator.calculate(tree, 'A', 'D') == 'A'  # 루트
```

#### 경로 전환 테스트
```python
# 초기 경로: A → B → D
path_service.switch_path('D', 'F')
assert store.active_path == ['A', 'B', 'F']

path_service.switch_path('F', 'G')
assert store.active_path == ['A', 'C', 'G']
```

#### Observer 테스트
```python
called = []
store.subscribe(lambda k, v: called.append((k, v)))

store.set_active_path(['A', 'B'])
assert ('active_path', ['A', 'B']) in called
```

#### 체크포인트 테스트
```python
store.set_active_path(['A', 'B', 'D'])
cp = checkpoint_service.create_checkpoint("before")

store.set_active_path(['A', 'C', 'G'])
checkpoint_service.restore_checkpoint(cp)

assert store.active_path == ['A', 'B', 'D']
```

---

## 📊 샘플 데이터 (픽스처)

### 기본 샘플 트리
```
    A (root: "대화 시작")
   / \
  B   C
 / \   \
D   F   G

노드 설명:
- A: system, "대화 시작"
- B: user, "진로 고민이 있어요"
- C: user, "인간관계 문제가 있어요"
- D: assistant, "진로에 대해 더 자세히 말씀해주세요"
- F: assistant, "먼저 본인의 흥미를 파악해보세요"
- G: assistant, "어떤 인간관계 문제인가요?"
```

**픽스처 제공**: `fixtures/sample_tree.py`에 `create_sample_tree()` 함수 구현

---

## 🚨 제약사항 및 주의사항

### 1. 의존성 방향
- **허용**: 상위 레이어 → 하위 레이어
- **금지**: 하위 레이어 → 상위 레이어
- **예**: Algorithm이 Business를 import하면 안 됨

### 2. Observer 패턴
- 리스너 에러는 전파하지 않음 (try-except 필수)
- `set_*` 메서드만 알림 발생 (직접 할당은 알림 없음)
- 순환 알림 주의 (리스너 내부에서 다시 `set_*` 호출 금지)

### 3. Memento 패턴
- Checkpoint는 완전히 불변 (`frozen=True`)
- `active_path`는 `tuple`로 저장 (list 아님)
- 복원 시 새 list 생성 (원본 수정 금지)

### 4. 성능
- LCA: O(h) (프로토타입 규모에서 충분)
- 노드 조회: O(1) (dict 기반)
- 경로 추적: O(h)
- **최적화는 필요 없음** (수십 개 노드만 다룸)

---

## 📅 일정 가이드

PM 승인 기간: **3-4주**

### Week 1: Data + Algorithm
- Node, ConversationTree
- Store (+ Observer)
- LCACalculator, PathReconstructor
- 알고리즘 정확성 100% 검증

### Week 2: Business
- PathSwitchService
- CheckpointService
- 통합 테스트

### Week 3: Presentation
- TerminalUI
- 명령어 핸들러
- 시각화 개선

### Week 4: Buffer
- 버그 수정
- 문서화
- 품질 보증

**품질 최우선** - 일정보다 완성도를 우선시

---

## 🔄 웹 전환 준비

### 재사용 가능한 컴포넌트
- `algorithms/` 전체 (LCA, PathReconstructor)
- `models/` 전체 (Node, Checkpoint)
- `data/tree.py` (ConversationTree)

### 교체할 컴포넌트
- `ui/terminal.py` → 웹 UI (React/Vue)
- `data/store.py` → Zustand/Redux

### 추가할 컴포넌트
- REST API (FastAPI)
- 데이터베이스 (MongoDB)
- AI 통합 (OpenAI API)

---

## 📞 지원 체계

### 막히는 부분이 있다면
1. **PM에게 문의**: 요구사항 불명확 시
2. **아키텍처 팀에게 문의**: 설계 해석 질문
3. **기술 총괄 판단**: 구현 방법 선택

### 설계 변경이 필요하다면
1. PM 승인 필요
2. 아키텍처 팀 검토
3. 트레이드오프 문서화

---

## 📚 참고 문서

- `/docs/아키텍처설계/터미널_프로토타입_설계서.md` (상세 설계)
- `/docs/아키텍처설계/아키텍처_판단기준.md` (의사결정 기준)
- `/docs/architecture/designPattern-deside.md` (디자인 패턴 가이드)

---

**인수인계 완료**

아키텍처 설계는 완료되었습니다. 이제 기술 총괄자가 구현을 진행하시면 됩니다.

질문이 있으시면 아키텍처 팀에 문의 바랍니다.

**행운을 빕니다! 🚀**

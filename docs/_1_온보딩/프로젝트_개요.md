# 프로젝트 개요

## 한 문장 요약

> **"트리 구조 대화 관리 시스템 - 여러 대화 경로를 동시에 관리하고, 참조하고, 병합할 수 있는 AI 상담 도구"**

---

## 기존 챗봇과의 차이점

| 기존 챗봇 | 우리 시스템 |
|----------|------------|
| 선형 대화 (한 방향) | 트리 구조 (분기 가능) |
| 이전 대화 수정 불가 | 노드 클릭으로 경로 전환 |
| 단일 컨텍스트 | 여러 경로 참조 가능 |
| 대화 리셋 시 처음부터 | 체크포인트로 저장/복원 |

---

## 핵심 컨셉 3가지

### 1. 트리 구조 대화

```
[Root]
  ├─ [질문1] → [답변1]
  │   ├─ [질문2a] → [답변2a]  ← 경로 A
  │   └─ [질문2b] → [답변2b]  ← 경로 B
  └─ [질문3] → [답변3]
```

### 2. 경로 전환

- 노드를 클릭하면 그 노드까지의 경로로 전환
- AI는 새 경로의 맥락으로 대화 재개
- 마치 파일 시스템에서 `cd` 명령어처럼 동작

### 3. 획기적인 기능들

- **크로스노드 참조**: 다른 경로의 내용을 현재 질문에 참조
- **분기 병합**: 두 대화 경로를 LCA 기반으로 통합
- **다중 관점**: 한 질문에 여러 AI 페르소나가 각자 답변

---

## 역할 체계

| 역할 | 책임 | 작업 공간 |
|------|------|-----------|
| PM | 일정/이슈 관리, 문서 관리, 온보딩 가이드 유지 | `_3_현재작업폴더/PM_관리/` |
| 아키텍처설계자 | 시스템 설계, 기술 의사결정, 개발 지시서 작성 | `_3_현재작업폴더/아키텍처설계/` |
| 백엔드 개발자 | 핵심 로직 구현, 테스트 작성 | `_3_현재작업폴더/초기개발/BackEnd/` |
| 프론트엔드 개발자 | UI 구현, 프로토타입 개발 | `_3_현재작업폴더/초기개발/FrontEnd /` |

---

## 프로젝트 현재 상태

Phase CLI-1 완료 (기본 트리 대화, 체크포인트, AI 통합). Phase UI-1 진행 중.

> 상세: `docs/_2_기능명세/기능_구현_상태.md`

---

## 레포지토리 구조

공개 레포(myDream)에 소스 코드와 기술 문서, 프라이빗 레포(myDream-private)에 사업 계획과 내부 작업 문서를 분리 운영한다. 공개 레포의 `.gitignore`에 프라이빗 폴더(`_3_`, `_4_`, `특허/` 등)가 등록되어 있다.

> 상세: `docs/git/레포지토리_분류.md`

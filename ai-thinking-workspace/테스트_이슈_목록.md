# UI 테스트 이슈 목록

> 테스트일: 2026-02-03
> 상태: 진행 중

---

## 발견된 이슈

### 1. 설정 드롭다운 - 레이아웃 설정 누락
- **위치**: 설정 아이콘(⚙️) 클릭 시 드롭다운
- **문제**: 기존에 있던 "레이아웃 설정" (사이드 패널 위치 변경) 메뉴가 없음
- **원인**: 모듈화 과정에서 기존 `app.js`의 `openLayoutModal()` 연결이 누락됨
- **수정 완료**:
  - `templates/settings-dropdown.html` - 레이아웃 설정 메뉴 추가
  - `js/module-d-dropdowns.js` - `openLayoutModal()` 핸들러 추가
  - `js/app.js` - `window.app` 전역 노출 추가
- **상태**: [x] 해결됨

### 2. "+ 새 작업공간" 버튼 클릭 - 무반응
- **위치**: 홈 화면 우측 상단 버튼
- **문제**: 클릭해도 아무 일도 안 생김
- **원인**: 모듈 간 연동 실패 - workspaceManager/router alias 없음
- **수정 완료**:
  - `js/module-b-home.js` - `window.workspaceManager`, `window.router` alias 추가
- **상태**: [x] 해결됨 (테스트 필요)

### 3. "마케팅 전략" 드롭다운 클릭 - 무반응
- **위치**: 헤더 좌측 작업공간 선택 드롭다운
- **문제**: 클릭해도 드롭다운이 열리지 않음
- **원인**: 모듈 간 연동 실패 - workspaceManager alias 없음
- **수정 완료**:
  - `js/module-b-home.js` - `window.workspaceManager` alias 추가로 모듈 C와 연동
- **상태**: [x] 해결됨 (테스트 필요)

### 4. 초기 화면에서 "마케팅 전략" 드롭다운 무의미
- **위치**: 헤더 좌측
- **문제**: 작업공간이 없는 상태에서 이미 선택된 것처럼 보임, 눈에 안 띔
- **분석**:
  - 민수: 멘탈 모델 불일치 - "아직 아무것도 안 만들었는데 왜 선택되어 있지?"
  - 수진: 시각적 고아 - 맥락 없이 혼자 존재
  - 동현: 기능 중복 - 홈 목록 + 드롭다운 = 같은 기능 2개
- **수정 완료**:
  - `js/module-b-home.js` - `setHomeHeaderState()` 추가: 홈에서 드롭다운 숨기기
  - `js/module-b-home.js` - `setWorkspaceHeaderState()` 추가: 작업공간에서 드롭다운 표시
- **상태**: [x] 해결됨 (테스트 필요)

### 5. 엣지 독 (생각 트리, 관점 도구) 위치/크기 이상
- **위치**: 좌우 엣지 독
- **문제**:
  - 엣지 독이 화면 아래로 내려감 (원래 중앙에 세로 배치)
  - 크기가 작아짐
- **원인**: `.workspace-view`에 flex 스타일이 없어서 `.app-main`의 높이가 제대로 설정되지 않음
- **수정 완료**:
  - `css/module-b-home.css` - `.workspace-view`에 flex 스타일 추가
- **상태**: [x] 해결됨 (테스트 필요)

### 6. 포크뷰 모달 안 열림
- **위치**: 생각 트리 하단의 "포크 뷰" 버튼
- **문제**: 클릭해도 포크뷰 모달이 열리지 않음
- **원인**: `initD3TreeView` 함수는 `tree.js`에 존재함 - 콘솔에서 에러 확인 필요
- **상태**: [ ] 미해결 (테스트 필요)

### 7. 브라우저 탭 제목 업데이트
- **위치**: 브라우저 탭
- **요청**: 작업공간 진입 시 브라우저 탭에 작업공간 이름 표시
- **수정 완료**:
  - `js/module-b-home.js` - `goWorkspace()`에서 `document.title` 업데이트
  - `js/module-b-home.js` - `goHome()`에서 `document.title` 업데이트
- **상태**: [x] 구현됨 (테스트 필요)

---

## 코드 레벨 원인 분석

### 모듈 간 연동 문제 (핵심 원인)

```
모듈 B: window.ModuleB
모듈 C: workspaceManager 를 찾음 → fallback 데이터 사용

해결 필요:
window.workspaceManager = window.ModuleB;
window.router = window.ModuleB;
```

### HTML ID 불일치 가능성

| 모듈 | 찾는 ID | 확인 필요 |
|------|---------|----------|
| B | `btn-new-workspace` | HTML에 존재하는지 |
| B | `home-workspace-list` | HTML에 존재하는지 |
| B | `home-empty-state` | HTML에 존재하는지 |
| C | `workspace-selector-btn` | HTML에 존재하는지 |

---

## 정상 작동 확인

### 첫 화면 (홈)
- [x] 헤더 좌측: 로고 + "마케팅 전략" 드롭다운
- [x] 헤더 우측: ⚙️ 설정 + [DK] 계정 아바타
- [x] 메인: "내 작업공간" + 태그라인
- [x] 검색창 + "+ 새 작업공간" 버튼

### 설정 드롭다운
- [x] 드롭다운 열림/닫힘
- [x] 테마 (라이트/다크) 표시
- [x] 알림 설정
- [x] 키보드 단축키
- [x] 도움말
- [ ] 레이아웃 설정 (누락)

### 계정 드롭다운
- [x] 드롭다운 열림/닫힘
- [x] 사용자 정보 (DK, 김대기, user@email.com)
- [x] 플랜 업그레이드
- [x] 개인 맞춤 설정
- [x] 설정
- [x] 도움말
- [x] 로그아웃 (빨간색)

---

## 추가 테스트 필요

- [ ] 작업공간 드롭다운 ("마케팅 전략" 클릭)
- [ ] 테마 토글 (라이트 ↔ 다크)
- [ ] 작업공간 내부 진입
- [ ] 엣지 독 (생각 트리, 관점 도구)
- [ ] 입력창 placeholder 변경
- [ ] 메시지 UI 스타일

---

*마지막 업데이트: 2026-02-03*

# 대화 트리 관리 CLI

대화를 트리 구조로 저장하고 관리하는 대화형 커맨드 라인 인터페이스입니다.

## 🎯 주요 기능

- **대화 트리 관리**: 대화를 트리 구조로 저장하고 분기점 생성
- **AI 통합**: OpenAI API를 통한 실제 AI 응답 생성 (선택적)
- **체크포인트 시스템**: 중요한 대화 지점에 이름표 붙이기
- **분기 자동 체크포인트**: 분기 발생 시 자동으로 체크포인트 생성
- **Navigation History**: 이동 이력 추적 및 되돌아가기
- **트리 시각화**: ASCII 아트로 대화 구조 시각화
- **노드 번호 시스템**: UUID 대신 n1, n2, n3... 형식으로 간편하게 참조

## 📦 설치

### 1. 의존성 설치

```bash
pip install -r requirements.txt
```

### 2. 환경 설정 (선택적 - AI 기능 사용 시)

```bash
# .env 파일 생성
cp .env.example .env

# OpenAI API 키 설정
echo "OPENAI_API_KEY=your-api-key-here" > .env
```

**참고**: API 키 없이도 CLI는 정상 작동하며, `turn` 명령으로 수동 대화 입력이 가능합니다.

## 🚀 시작하기

### CLI 실행

```bash
python -m cli.cli
```

또는

```bash
python cli/cli.py
```

### 첫 명령어

```
> help
```

모든 사용 가능한 명령어와 설명을 확인할 수 있습니다.

## 📖 기본 사용법

### 1. AI에게 질문하기 (AI 통합 사용 시)

```bash
> ask Python이란?
💭 AI에게 질문 중...
✅ 대화 턴이 추가되었습니다.
```

### 2. 수동 대화 입력

```bash
> turn Python이란? | Python은 프로그래밍 언어입니다.
✅ 대화 턴이 추가되었습니다. (노드 ID: abc123...)
```

**형식**: `turn <질문> | <답변>`

### 3. 대화 트리 확인

```bash
> tree
root
└─ n1 👉 "Python이란?"
```

- `👉`: 현재 위치
- `n1, n2, ...`: 노드 번호

### 4. 현재 대화 내용 보기

```bash
> show
[현재 노드]
질문: Python이란?
답변: Python은 프로그래밍 언어입니다.
```

## 🌳 분기 생성

대화 트리의 핵심 기능은 **분기(branching)**입니다. 이전 대화 지점으로 돌아가 새로운 질문을 할 수 있습니다.

### 예시: 분기 만들기

```bash
# 1. 첫 대화
> ask Python이란?
✅ 노드 생성됨: n1

# 2. 후속 질문
> ask Django는?
✅ 노드 생성됨: n2

# 3. n1로 돌아가기
> switch n1
✅ 노드 n1로 전환

# 4. 다른 질문하기 (분기 생성!)
> ask Flask는?
🔀 분기 발생: 자동 체크포인트 '@branch_abc12345' 생성됨
✅ 노드 생성됨: n3

# 5. 트리 구조 확인
> tree
root
└─ n1 "Python이란?" [@branch_abc12345]
   ├─ n2 "Django는?"
   └─ n3 👉 "Flask는?"
```

### 분기 자동 체크포인트

노드에 2번째 자식이 생성되는 순간, 해당 노드에 **자동으로 체크포인트**가 생성됩니다:
- 이름 형식: `@branch_<node_id>`
- 나중에 `checkpoint load @branch_abc12345` 명령으로 쉽게 복귀 가능

## 📋 명령어 레퍼런스

### 대화 관리

#### `ask <질문>`
AI에게 질문하고 응답을 받습니다.

```bash
> ask Python의 장점은?
💭 AI에게 질문 중...
✅ 대화 턴이 추가되었습니다.
```

**참고**: AI 통합이 비활성화되어 있으면 에러 메시지가 표시됩니다.

#### `turn <질문> | <답변>`
수동으로 대화 턴을 추가합니다.

```bash
> turn React란? | React는 UI 라이브러리입니다.
✅ 대화 턴이 추가되었습니다.
```

#### `history`
현재 경로의 전체 대화 히스토리를 표시합니다.

```bash
> history
================================================================================
현재 경로의 대화 히스토리 (3턴):
================================================================================

[턴 1]
Q: Python이란?
A: Python은 프로그래밍 언어입니다.
--------------------------------------------------------------------------------

[턴 2]
Q: Django는?
A: Django는 웹 프레임워크입니다.
--------------------------------------------------------------------------------

[턴 3]
Q: REST API는?
A: REST API는 아키텍처 스타일입니다.
--------------------------------------------------------------------------------
```

#### `show`
현재 노드의 대화 내용을 표시합니다.

```bash
> show
[현재 노드]
질문: Python이란?
답변: Python은 프로그래밍 언어입니다.
```

### 노드 이동

#### `switch <참조>`
특정 노드로 이동합니다.

**참조 형식**:
- 노드 번호: `n1`, `n2`, `n3`, ...
- 노드 ID: `abc12345` (전체 또는 일부)

```bash
# 노드 번호로 이동
> switch n1
✅ 노드 n1로 전환

# 노드 ID로 이동 (부분 매칭)
> switch abc
✅ 노드로 전환되었습니다.
```

**이동 이력 저장**: `switch` 실행 시 현재 위치가 navigation history에 자동 저장됩니다.

#### `back`
이전에 방문했던 위치로 복귀합니다.

```bash
> switch n5
✅ 노드 n5로 전환

> switch n3
✅ 노드 n3로 전환

> back
✅ 이전 위치로 돌아갔습니다.
   질문: (n5의 질문)
```

**이력 관리**:
- 최근 20개 위치만 유지
- root 노드는 이력에 저장되지 않음

#### `visits`
최근 방문 이력을 표시합니다.

```bash
> visits
📜 최근 방문 이력 (5개):
================================================================================
  1. n5 - Python의 장점은? (방금 전)
  2. n3 - Flask는? (2분 전)
  3. n1 - Python이란? (5분 전)
  4. n8 - React Hooks는? (10분 전)
  5. n6 - JavaScript란? (15분 전)
================================================================================
💡 사용: back (최근 위치로 복귀)
```

### 체크포인트

체크포인트는 노드에 붙이는 **이름표**입니다. 중요한 대화 지점을 쉽게 찾고 돌아갈 수 있습니다.

#### `checkpoint save <이름>`
현재 노드에 체크포인트를 저장합니다.

```bash
> checkpoint save 실험1
✅ 체크포인트 '실험1'이 저장되었습니다.
```

**별칭**: `cp save <이름>`

**이름 규칙**:
- 영문, 숫자, 언더스코어(`_`), 하이픈(`-`)만 사용 가능
- 최대 50자
- 공백 및 특수문자 불가

#### `checkpoint load <이름>`
체크포인트로 이동합니다.

```bash
> checkpoint load 실험1
✅ 체크포인트 '실험1'으로 이동했습니다.
```

**별칭**: `cp load <이름>`

**이동 이력 저장**: `checkpoint load` 실행 시 현재 위치가 navigation history에 자동 저장됩니다.

#### `checkpoint list`
저장된 모든 체크포인트를 3개 섹션으로 표시합니다.

```bash
> checkpoint list

📋 체크포인트 & 이력:
================================================================================

[명시적 체크포인트] (2개)
  • 실험1
    질문: Python이란?
    깊이: 1 | 자식: 2개

  • 중요지점
    질문: Django REST API 설계
    깊이: 3 | 자식: 1개

[분기 노드] (2개) 🔀
  • @branch_abc12345 → n1
    질문: Python이란?
    자식: 2개 (분기점)

  • @branch_def67890 → n6
    질문: JavaScript란?
    자식: 2개 (분기점)

[최근 방문] (5개) 📜
  • n8 - React Hooks는? (5분 전)
  • n3 - Flask는? (8분 전)
  • n1 - Python이란? (13분 전)
  ... 외 2개
  💡 전체 보기: visits

================================================================================
```

**별칭**: `cp list`

#### `checkpoint delete <이름>`
체크포인트를 삭제합니다.

```bash
> checkpoint delete 실험1
✅ 체크포인트 '실험1'이 삭제되었습니다.
```

**별칭**: `cp delete <이름>`

### 트리 탐색

#### `tree [옵션]`
대화 트리를 시각화합니다.

```bash
> tree
root
└─ n1 "Python이란?" [@branch_abc12345] [실험1]
   ├─ n2 "Django는?"
   │  └─ n3 "REST API는?"
   └─ n4 👉 "Flask는?"
      └─ n5 "Blueprint는?"
```

**옵션**:
- `nopath`: 현재 경로 강조 제거
- `nocp`: 체크포인트 표시 제거
- `depth=N`: 표시 깊이 제한 (예: `depth=3`)

```bash
# 깊이 2까지만 표시
> tree depth=2
root
└─ n1 "Python이란?"
   ├─ n2 "Django는?"
   └─ n4 "Flask는?"
```

#### `path [content]`
현재 노드까지의 경로를 표시합니다.

```bash
> path
현재 경로 (깊이: 3):
root → n1 → n2 → n3 (현재)

# 내용 포함
> path content
현재 경로 (깊이: 3):
root → Python이란? → Django는? → REST API는? (현재)
```

#### `nodes` / `list`
모든 노드를 번호와 함께 나열합니다.

```bash
> nodes
전체 노드 목록 (10개):
  n1: Python이란?
  n2: Django는?
  n3: REST API는?
  n4: Flask는?
  n5: Blueprint는?
  n6: JavaScript란?
  n7: React는?
  n8: Hooks는?
  n9: Vue는?
  n10: 데이터베이스란?
```

#### `node [참조]`
특정 노드의 상세 정보를 표시합니다.

```bash
> node n1
[노드 n1 상세 정보]
ID: abc12345-6789-...
질문: Python이란?
답변: Python은 프로그래밍 언어입니다.
자식 노드: 2개 (n2, n4)
체크포인트: @branch_abc12345, 실험1
```

#### `siblings [참조]`
형제 노드를 표시합니다.

```bash
> siblings n2
[n2의 형제 노드]
  • n2 👉 "Django는?" (현재)
  • n4 "Flask는?"
```

#### `stats`
대화 트리 통계를 표시합니다.

```bash
> stats
=== 대화 트리 통계 ===
전체 노드: 10개
최대 깊이: 4
전체 분기: 2개
체크포인트: 4개
```

### 기타

#### `help`
사용 가능한 모든 명령어를 표시합니다.

```bash
> help
```

슬래시(`/`) 접두사는 선택적입니다:
- `help` ✅
- `/help` ✅

#### `exit` / `quit`
CLI를 종료합니다.

```bash
> exit
CLI를 종료합니다.
```

## 🎓 사용 시나리오

### 시나리오 1: 기본 대화 흐름

```bash
# 1. CLI 시작
python -m cli.cli

# 2. 첫 질문
> ask Python이란?
✅ 노드 생성됨: n1

# 3. 내용 확인
> show
[현재 노드]
질문: Python이란?
답변: ...

# 4. 트리 확인
> tree
root
└─ n1 👉 "Python이란?"

# 5. 후속 질문
> ask Django는?
✅ 노드 생성됨: n2

> tree
root
└─ n1 "Python이란?"
   └─ n2 👉 "Django는?"
```

### 시나리오 2: 분기 생성 및 탐색

```bash
# 1. 초기 대화 (n1 → n2 → n3)
> ask Python이란?
> ask Django는?
> ask REST API는?

# 2. n1로 돌아가기
> switch n1
✅ 노드 n1로 전환

# 3. 새로운 질문 (분기 생성!)
> ask Flask는?
🔀 분기 발생: 자동 체크포인트 '@branch_abc12345' 생성됨
✅ 노드 생성됨: n4

# 4. 트리 구조 확인
> tree
root
└─ n1 "Python이란?" [@branch_abc12345]
   ├─ n2 "Django는?"
   │  └─ n3 "REST API는?"
   └─ n4 👉 "Flask는?"

# 5. 다른 분기로 이동
> switch n3
✅ 노드 n3로 전환

# 6. 되돌아가기
> back
✅ 이전 위치로 돌아갔습니다.
   질문: Flask는?
```

### 시나리오 3: 체크포인트 활용

```bash
# 1. 중요한 지점에서 체크포인트 저장
> checkpoint save 실험1
✅ 체크포인트 '실험1'이 저장되었습니다.

# 2. 여러 질문 진행...
> ask 추가질문1?
> ask 추가질문2?
> ask 추가질문3?

# 3. 체크포인트 목록 확인
> checkpoint list

[명시적 체크포인트] (1개)
  • 실험1 → n4
    질문: Flask는?
    깊이: 2 | 자식: 1개

[분기 노드] (1개) 🔀
  • @branch_abc12345 → n1
    질문: Python이란?
    자식: 2개 (분기점)

# 4. 체크포인트로 복귀
> checkpoint load 실험1
✅ 체크포인트 '실험1'으로 이동했습니다.

# 5. 분기 체크포인트로 복귀
> checkpoint load @branch_abc12345
✅ 체크포인트 '@branch_abc12345'로 이동했습니다.
```

### 시나리오 4: Navigation History 활용

```bash
# 1. 여러 노드를 탐색
> switch n5
> switch n3
> switch n8
> switch n1

# 2. 방문 이력 확인
> visits
📜 최근 방문 이력 (4개):
  1. n8 - ...
  2. n3 - ...
  3. n5 - ...
  4. n1 - ... (최초)

# 3. 한 단계씩 되돌아가기
> back
✅ 이전 위치로 돌아갔습니다. (n8)

> back
✅ 이전 위치로 돌아갔습니다. (n3)

> back
✅ 이전 위치로 돌아갔습니다. (n5)
```

## ⚙️ 고급 기능

### 1. 분기 자동 체크포인트

노드에 2번째 자식이 생성되는 순간, 자동으로 체크포인트가 생성됩니다.

**동작 원리**:
```
현재 노드의 자식 개수 >= 1
→ 새 노드 추가 시 자식이 2개가 됨
→ 분기 발생!
→ 자동 체크포인트 생성: @branch_<node_id>
```

**장점**:
- 수동으로 체크포인트를 만들지 않아도 됨
- 모든 분기점이 자동으로 기록됨
- 나중에 쉽게 분기점으로 복귀 가능

**예시**:
```bash
> ask Python이란?          # n1 생성
> ask Django는?            # n2 생성 (n1의 첫 자식)
> switch n1
> ask Flask는?             # n4 생성 (n1의 두 번째 자식)
🔀 분기 발생: 자동 체크포인트 '@branch_abc12345' 생성됨

# 나중에...
> checkpoint load @branch_abc12345
✅ 분기점으로 복귀
```

### 2. Navigation History

`switch`와 `checkpoint load` 명령으로 노드를 이동할 때마다 현재 위치가 자동으로 기록됩니다.

**특징**:
- 최근 20개 위치만 유지 (메모리 절약)
- root 노드는 기록하지 않음
- 상대 시간 표시 ("방금 전", "5분 전", "2시간 전")

**명령어**:
- `visits`: 최근 방문 이력 보기 (최근 10개)
- `back`: 이전 위치로 복귀

**사용 예시**:
```bash
# 여러 노드 탐색
> switch n5    # n3 → n5 (n3 이력 저장)
> switch n8    # n5 → n8 (n5 이력 저장)
> switch n2    # n8 → n2 (n8 이력 저장)

# 이력 확인
> visits
📜 최근 방문 이력 (3개):
  1. n8 - ... (방금 전)
  2. n5 - ... (1분 전)
  3. n3 - ... (2분 전)

# 단계별 되돌아가기
> back         # n2 → n8
> back         # n8 → n5
> back         # n5 → n3
```

### 3. 노드 번호 시스템

UUID 대신 `n1`, `n2`, `n3`... 형식으로 노드를 참조할 수 있습니다.

**번호 부여 규칙**:
- 전위 순회(pre-order) 순서
- root 제외
- 트리 구조가 변경되면 번호도 변경될 수 있음

**사용 가능한 명령어**:
- `switch n1`
- `node n5`
- `siblings n3`

**혼합 사용 가능**:
```bash
> switch n1        # 노드 번호
> switch abc123    # 노드 ID (전체 또는 일부)
```

### 4. 슬래시 선택적 명령어

모든 명령어는 슬래시(`/`) 접두사를 생략할 수 있습니다:

```bash
# 두 방식 모두 동일
> help
> /help

> ask Python이란?
> /ask Python이란?

> tree
> /tree
```

## 🛠️ 트러블슈팅

### 문제 1: AI 기능이 작동하지 않음

**증상**:
```bash
> ask Python이란?
❌ AI 클라이언트를 사용할 수 없습니다.
```

**해결책**:
1. `.env` 파일에 `OPENAI_API_KEY` 설정 확인
2. API 키가 유효한지 확인
3. 인터넷 연결 확인

**대안**: `turn` 명령으로 수동 입력
```bash
> turn Python이란? | Python은 프로그래밍 언어입니다.
```

### 문제 2: 체크포인트를 찾을 수 없음

**증상**:
```bash
> checkpoint load cp1
❌ 체크포인트 'cp1'을 찾을 수 없습니다.
```

**해결책**:
1. `checkpoint list`로 저장된 체크포인트 확인
2. 이름 철자 확인 (대소문자 구분)

### 문제 3: back 명령이 작동하지 않음

**증상**:
```bash
> back
❌ 이동 이력이 없습니다.
```

**원인**:
- `switch` 또는 `checkpoint load`를 사용하지 않음
- CLI를 재시작한 경우 (이력은 메모리에만 저장됨)

**해결책**:
`switch` 명령으로 노드를 이동한 후 `back` 사용

### 문제 4: 노드 번호가 변경됨

**원인**:
노드 번호는 트리 구조가 변경될 때마다 재계산됩니다 (전위 순회 순서).

**해결책**:
- 중요한 노드는 체크포인트로 저장
- 또는 노드 ID (UUID)를 사용

## 📂 파일 구조

```
.
├── cli/
│   └── cli.py              # CLI 메인 파일
├── core/
│   ├── models.py           # Node, Tree 클래스
│   ├── store.py            # Store 클래스
│   ├── conversation.py     # ConversationManager
│   ├── checkpoint.py       # 체크포인트 유틸리티
│   └── path_utils.py       # 경로 유틸리티
├── ai/
│   └── client.py           # OpenAI API 클라이언트
├── tests/                  # 테스트 파일
├── .env                    # 환경 변수 (생성 필요)
├── .env.example            # 환경 변수 템플릿
├── requirements.txt        # Python 의존성
└── README_CLI.md           # 본 문서
```

## 🧪 테스트

단위 테스트 실행:

```bash
pytest tests/
```

특정 테스트 파일 실행:

```bash
pytest tests/test_models.py
pytest tests/test_store.py
```

시나리오 테스트 실행:

```bash
python test_scenario_4.py    # 대규모 트리 테스트
python test_scenario_5.py    # Navigation history 테스트
```

## 📝 추가 정보

### 데이터 영속성

현재 버전에서는 **메모리 내 저장**만 지원합니다:
- CLI 종료 시 모든 데이터 손실
- Navigation history도 메모리에만 저장

**향후 계획** (Phase CLI-2/3):
- 파일 기반 저장소
- 세션 복원 기능
- Navigation history 영속화

### 제한사항

- **최대 노드 수**: 제한 없음 (메모리 허용 범위 내)
- **Navigation history**: 최근 20개만 유지
- **체크포인트 이름**: 최대 50자
- **AI 응답 길이**: 최대 1024 토큰

### 성능 권장사항

- 노드 1000개 이하: 원활한 성능
- 노드 1000~5000개: 트리 시각화 시 약간의 지연
- 노드 5000개 이상: `tree depth=N` 옵션으로 깊이 제한 권장

## 🤝 기여

버그 리포트 및 기능 제안은 이슈 트래커를 통해 제출해주세요.

## 📄 라이선스

이 프로젝트는 Phase CLI-1 개발의 일부입니다.

---

**작성일**: 2025-12-02
**버전**: 1.0.0
**Phase**: CLI-1
